# 开发待办与交接

更新：2026-09-23。昨日提交基线：`ee1dacc`（功能基线 `f8af66b`）。勾选项已完成对应工程验证，未勾选项仍待开发/验收；不改变现有 M0 研究验收状态。

当前核实：本轮约定的独立 Analysis Agent 尚未实现，也未接在统一证据包之后。已有 `agents/analysis/` 是历史分析实验（直接读文、analyst/reviewer、年报报告），不能当作新双 Agent 链路。当前 query 报告是确定性取证报告；`semantic_completeness` 仍为 `not_evaluated`。本轮端到端验收范围是 Data Agent 从问题、任务、工具、完成检查到证据报告；完整研究分析仍属于第 4 项。

目录整理已完成：[Agent 职责结构](docs/CODE_STRUCTURE.md)、[工作台页面/组件/静态资源结构](apps/review_workbench/README.md)。统一回归 661 项 Python + 7 项 JavaScript 通过；[七例离线端到端结果及真实模型配置](experiments/data_agent_query/e2e-review-v1/README.md)。真实模型混合问题 79/79 + 23.69%、合成 Other 3/3 + 800 USD 已通过；缺资料首次无进展，修正规划说明后 1 次调用返回澄清。当前账本累计 46/48 次（本轮新增 20 次），旧费用、失败和冻结产物保留。各运行版本与边界见端到端报告。

G0 首轮用户意见已记录：[审核草稿](data/evaluation/m0_r2a/reviews/2026-09-23-01/REVIEW.zh-CN.md)。用户表达业务细分与收入聚合应分开、提取缺失值保持未知；节点定义仍需讨论，第 90 段来源处置尚未理解/确认。拟做同源材料多模型匿名对照，准确模型 ID/渠道尚待明确，未调用模型。原候选、Gold 与来源采纳状态不变。

## 接下来先完成什么

每项提供可由研究者核对的交付，至少展示：**输入、执行条件、实际动作、实际返回、检查结果与缺口**。Agent 检查数据结构与代码逻辑，页面视觉 review 交用户；必要检查通过即可交付并推进已授权工作，不等待视觉确认。工程测试通过不等于研究验收完成。

当前证据包的只读验收入口：`/companies/alphabet/data/acceptance?collection=evidence-package-v1`（也可从「结构化数据 → 步骤验收」进入）。已实现输入表、六步回放、原话/字段对照、SQL/计算与证据缺口；这是年报 + 电话会的离线工具样例，不是下方年报大问题的完整验收。第 3 项已完成 3.1 任务计划、3.2 取证进度和 3.3 完成检查，新增[逐步判定报告](experiments/data_agent_query/task-completion-v1/offline-02/README.md)；现已接通任务循环与直接 query 入口，真实 DeepSeek 验收达到 Business 79/79，并完成带引用的营业利润率查询；[实际运行报告](experiments/data_agent_query/task-query-canary-v1/README.md)。页面未改，完整业务/风险大问题仍未验收。

先跑通一个包含文档阅读与取数的大问题，再验证跨公司迁移：

> 根据 Alphabet FY2025 10-K，分析重点业务和关键风险，并提供可核查依据。

这是显式登记的验收样例，不得成为通用接口的默认公司或期间。只使用选定年报及其版本；不能混入后续季度、电话会或网络材料。

| 顺序 | 待办 | 产物与验收标准 |
| --- | --- | --- |
| 1 | [x] 接通章节阅读与统一来源范围 | `run_scoped()` 接入目录、章节读取/续读和字面搜索；SQL、计算、证据与覆盖诊断共用显式来源范围。越界、缺章节及超大完整组返回错误或缺口；保留旧快照。[完成报告](docs/DATA_AGENT_SOURCE_SCOPE_REPORT.zh-CN.md) · [7 步离线样例](experiments/data_agent_query/source-scope-v1/offline-01/README.md)。 |
| 2 | [x] 定义统一证据包 | 新增 `retrieval-evidence-v1`：原文、表格、引文、图片引用、归一化记录、计算与模型提取/摘要分别分型。自动保存证据包/Schema，核验冻结来源和派生关系，区分正文/出处/导航/图片覆盖；历史快照不变。[完成报告](docs/DATA_AGENT_EVIDENCE_PACKAGE_REPORT.zh-CN.md) · [可读产物](experiments/data_agent_query/evidence-package-v1/offline-01/evidence-report-v2.md)。 |
| 3 | [ ] 补齐任务拆解与 ReAct 完成条件 | 按下方 3.1–3.6 实施：任务计划 → 取证进度 → 完成检查 → 补查循环 → 可读报告 → 离线验收。区分工程取证条件满足与语义分析完成，不能仅凭模型 `finish` 声称全文覆盖。先验证顺序执行。 |
| 4 | [ ] 验收上述完整问题 | 冻结来源、验收问题和检查表，先离线验证工具，再开展另行明确预算的真实模型验收。检查 Business、Risk Factors、MD&A 和相关附注的阅读范围，核对业务/风险分析证据、数值口径、遗漏和重复。保存逐步过程、证据包、分析结果与未完成项，报告模型声明与实际覆盖的差异。 |
| 5 | [ ] 验证跨公司泛化 | 建立显式适配器注册和能力声明，逐步解除构建器对 Alphabet 适配器/指标目录的直接依赖。事先登记至少一家不同财年公司和一家不同业务结构公司，从原文完整跑到入库与查询。检查币种/尺度、财年、分部维度、指标定义、证据和遗漏；未知映射保持 unresolved。查询层的合成数据测试不能代替端到端验收。 |

Analysis Agent 负责研究维度、业务重要性与风险影响判断；Data Agent 负责拆分证据需求、读取/取数/处理及覆盖说明。Text2SQL 是其中一项能力，任务可同时消费文本、表格和图片引用。

2026-09-23 的样例使用预设步骤验证工具：目录 → 业务正文 → 续读 → 风险搜索 → 风险正文 → SQL/计算 → 引用汇总。随后完成统一证据包，并新增包含图片引用、模型原始提取、归一化记录与计算的离线样例。这些离线样例未调用真实模型，未完成第 4 项的大问题验收；`answered` 仅表示引用型工具回路完成，不能理解为全文阅读或研究完成。随后实现按任务归属的进度记录和工程完成检查，3.4 的基础循环及自动拆题入口已接通，并用离线规划器验证补查与结束检查。首次真实计划请求超时；随后真实两题通过，数字题曾因规划范围过大失败并已修正。计划在执行中修订仍待实现。

## 第 3 项实施清单：任务拆解、取证进度与缺口

起点：章节阅读、统一来源范围、证据包和只读步骤验收页已经可用。3.1–3.3 已实现独立顺序执行、进度与工程判定，沿用 Python/Pydantic、DocumentReader 和受约束 SQL/DuckDB；3.4 已新增规划器循环入口，使用相同 planner.decide 协议和现有受计费模型适配器。上一轮全量 642 项 Python、7 项 JavaScript 检查通过，边界见[完成检查报告](docs/DATA_AGENT_TASK_COMPLETION_REPORT.zh-CN.md)。

| 顺序 | 待办 | 具体产物与验收条件 |
| --- | --- | --- |
| 3.1 | [x] 定义任务计划契约 | 新版本 `TaskPlan`：每个子任务记录要回答的事项、所需证据类型、明确来源/期间或待澄清项、依赖和可检查的完成条件。章节 ID 从真实目录取得。校验任务 ID 唯一、依赖存在且无环、来源不越界；计划不得隐式使用示例公司或猜测期间。保留计划来源；当前调用方/预设计划可执行，模型拆解接入留待 3.4，不宣称 Schema 能证明需求拆解完整。 |
| 3.2 | [x] 实现由程序维护的取证进度 | 每次工具执行关联任务 ID；宿主程序根据实际请求和已核验返回结果更新正文块、证据产物 ID、续读位置及错误。按 snapshot/source/index/block 区分、去重，计算指定范围内的已返回/未返回块；目录、搜索预览、provenance-only 和图片引用不计作正文阅读。模型不能直接写入已读集合或完成状态。 |
| 3.3 | [x] 实现完成检查 | 支持“指定正文范围已返回”“所需记录及引用已取得”“计算依赖齐全且计算成功”等代码检查；保留条件/反例等语义检查的独立评估状态。输出条件满足、仍缺证据、执行受阻或达到上限及具体原因；依赖未满足时不能完成后续任务。已提供只读 `check_completion()` 和结束资格；模型 `finish` 接入留待 3.4，不允许直接写完成状态。 |
| 3.4 | [ ] 接入按任务补查的 ReAct 循环 | 模型接收当前任务、实际进度和缺口，选择已有阅读/搜索/查询工具继续执行；按依赖顺序推进。记录补查、失败纠正和无进展重复。保留步数、上下文和费用上限；达到限制时保存未完成任务和游标。计划调整保存新版本及原因，不得通过删任务、缩小完成条件、换运行目录或重建账本伪装完成或重置额度。 |
| 3.5 | [ ] 完善可读任务报告（query 报告已实现） | 表格展示“问题事项 → 所需证据 → 实际取得内容 → 未读范围/缺口 → 当前状态”，可追到工具步骤和证据包。每轮保存不可变状态快照与事件记录，支持离线回放。报告把代码检查结果、模型语义判断及人工验收分开，不用全文块数百分比代表问题完成度。 |
| 3.6 | [ ] 完成离线验收 | 用独立合成公司和真实冻结材料分别验证：条件满足、部分完成、来源/章节缺失、执行上限、重复读取、无效依赖、跨来源块 ID 碰撞及模型过早 `finish`。核验进度可回放、缺口不会无依据消失、旧快照/旧结果可读；运行完整回归并保存逐步报告。真实模型能否自主拆对问题、找全证据，留到第 4 项另行验收。 |

**3.1 + 3.2 已完成：**[六步可读样例](experiments/data_agent_query/task-progress-v1/offline-01/README.md)逐步展示输入、条件、返回和缺口。

1. 从调用方明确选择的材料中取得目录，登记一个指定章节范围的读取任务，以及一个取数/计算任务；测试计划来自显式 fixture。
2. 第一批正文返回后，能看到准确的已返回块、剩余范围和续读位置；取数结果能连接到已有证据包。
3. 继续读取同一章节，进度合并且重复块不重复计数；搜索预览不会改变正文进度。
4. 保存计划、每轮状态和工具记录，在临时目录离线重放得到相同进度；本次一致性重放已通过；该样例仍按原规则保留未评估状态，新的完成检查另存新运行。

**3.3 已完成并验证的问题：**“已拿到数据”如何转为可核查的工程条件，而不等同于“研究完成”？

- 输入：TaskPlan、宿主维护的逐步进度和已校验证据包。
- 检查：范围可用且正文未读集合为空；所需期间/指标的记录与引用齐全；计算依赖完整且计算成功；前置任务条件满足。
- 输出：每项条件的满足/缺口/受阻状态、证据 ID 和具体原因。语义充分性独立保持未评估。
- 反例：读到末尾但前文未读、记录存在但引用缺失、重复返回、依赖未满足、提前 finish、达到步数上限，均不能伪装完成。
- 不改用新的材料、不接付费模型；先离线验证后接入 3.4。核心技术卡点先定义问题并同步，不自主查询 内部公司系统。

完成检查的[六步产物](experiments/data_agent_query/task-completion-v1/offline-02/README.md)：业务正文仍缺 72 块；数字任务的记录、原文引用和计算条件满足；整体在六步上限停止并保留缺口。已同时验证独立合成公司的正反例与旧运行重放，详见[报告](docs/DATA_AGENT_TASK_COMPLETION_REPORT.zh-CN.md)。

**3.4 本轮只推进一个可见结果：**显式任务计划 → 根据实际缺口选工具 → 执行 → 检查 → 继续/结束。该基础循环已接通，未勾选整个 3.4。

1. 给规划器提供明确计划、当前任务、实际进度、缺口和剩余额度；工具动作绑定任务/要求 ID。
2. 宿主校验依赖和来源，执行已有工具，将核验后的进度与完成检查返回下一轮。
3. 模型提出 finish 时只请求程序检查；未满足则反馈缺口，上限到达则停止并保存未完成项。保留语义未评估。
4. 先用预设规划器验证提前 finish、重复无进展、错误纠正及依赖推进，再安排另行授权的真实模型验收。不得借更换运行目录或账本重置已有额度。

本轮基础闭环结果（[报告](experiments/data_agent_query/task-react-v1/offline-01/README.md)）：初次 finish 被拒绝；按实际未读位置续读，正文 **0 → 10 → 22 → 32 → 45 → 61 → 73 → 79/79**；数字任务取得记录/引用/计算后 finish 通过。共 10 次模拟规划决策、8 次工具执行，0 次模型调用。44 项定向测试通过（约 11 秒）；本轮未重跑全量或页面验收。

上述离线样例使用固定调用方计划。后续已接通自动拆题及真实模型执行；仍仅最新工具返回与累计进度进入规划器上下文，完整原文保存在逐步产物。计划修订/迁移、跨运行恢复和最终分析报告尚未实现。

新增 query 入口：`DataQueryAgent.run_question()` 接收原问题及已选来源范围，模型自动提出任务并调用既有任务循环，用户不需手写 TaskPlan。真实 DeepSeek `deepseek-flash` 两题已跑通：[运行报告](experiments/data_agent_query/task-query-canary-v1/README.md)。Business 正文 79/79（10 次模型调用）；数字题修正“附引用被扩大为全文阅读”的规划错误后，3 次调用返回收入、营业利润、23.69% 利润率及引用。历史失败运行保留，累计 26 次请求（25 次成功响应、1 次超时）。新增自动可读报告及运行进度日志；32 项定向检查通过。费用按用户最新要求记录、不阻断本轮开发，沿用原账本；AiHubMix 变量名兼容已修复，但本轮未调用。

迭代约定：先交付可观察的行为变化，再做对应定向检查；不再把测试总数或大篇幅技术报告作为主要验收成果。下一步用同时包含文本与数字的问题检验任务拆分，并验证另一家公司及缺资料场景；沿用用户最新授权和显式运行限制，不再扩充未用到的架构。

该阶段的阅读范围由问题及显式计划决定，不要求每个问题都读完整份年报。只取得少量片段时，仍须保留未读范围和未满足要求。模型对“证据是否足够”的语义判断不能替代实际执行记录。

当前 TaskProgressSession 按显式请求顺序执行，逐轮保存 JSON 事件、证据包与状态；新运行显式启用完成检查后，依赖任务必须等待前置任务工程条件满足。判定与进度分开保存，旧运行按原规则重放。新增 `DataQueryAgent.run_tasks()` 通过此 Session 执行规划器动作；`SdkQueryPlanner(task_mode=True)` 使用新动作契约。已有 run/run_scoped 行为不变，语义充分性仍未评估。DuckDB 继续负责财务查询。当前阶段不新增网络搜索、OCR、来源采集或自动采纳；本轮真实模型小样本已授权完成，数字与正文两题通过。

## 后续排队

- [ ] 新数据更新批次：来源版本登记 → 新增/修订/重复/冲突差异 → 校验 → 显式发布候选快照。保留历史输入，新快照不自动替换旧分析。
- [ ] 在更新批次验收后增加来源发现、增量获取与调度；当前没有自动增量同步。
- [ ] 按真实问题接入图片读取、OCR/视觉理解及高质量外部来源；保留原图与派生产物的对应关系。
- [ ] 分别评估解析遗漏、提取语义保真和来源可追溯性。引用匹配通过、块数或数据库行数均不能代替全文召回率。

## 当前已完成的基线

- [x] 参考 Claude Financial Services 改写抽取 prompt，接入 DeepSeek；保留原话、期间、尺度、条件和 Q&A 归属。
- [x] 建立 DuckDB/Schema、受约束查询计划及多轮工具反馈；当前仍有财年、币种和适配器范围限制。
- [x] 保存 4 份来源、37 条候选记录、52 个证据对象及两轮自然语言查询样例；不是全文结构化或所有用例均通过。
- [x] 展示问题、SQL 和实际结果；提供数据库表格、Schema、逐行溯源、信息损耗与更新机制说明。
- [x] 禁止共享接口中的隐式业务默认值；增加来源/公司隔离、精度、证据和快照校验。
- [x] 504 项 Python、7 项 JavaScript 检查通过，已提交个人 GitHub。工程检查通过不代表研究采纳或泛化验收。

## 换机器后从这里开始

在个人仓库检出目录中执行；有本地未提交改动时先核对，避免覆盖：

```bash
git pull --ff-only
uv sync --locked --extra analysis --extra data
.venv/bin/python scripts/check_workspace.py
.venv/bin/python scripts/run_offline_checks.py --integration
```

默认 `run_offline_checks.py` 只跑既有 portable core，未包含全部新增 Data Agent 用例；上面的 `--integration` 包含完整 Python 回归和 JavaScript 检查。离线检查、查看历史数据与 UI 不需要 API Key。

工作区检查通过后启动：

```bash
PYTHONPATH=src:. .venv/bin/python -m apps.review_workbench.app --host 127.0.0.1 --port 8765
```

- 数据库与溯源：`/companies/alphabet/data/dataset?collection=nl-canary-v1`
- 查询样例：`/companies/alphabet/data/queries?collection=nl-canary-v1&run=live-02&case=q1-margin`
- 当前查询快照：`query-c9e2d02426106321794973a3`，目录为 `experiments/data_agent_query/2026-09-22-pilot-04/dataset`。
- 仓库已包含部分公开来源、索引、查询快照和历史运行；其它未提交资料以检查结果为准，不假设全量历史工作区已恢复。旧 `review_excerpt.json` 第 90 段仍有已知指纹问题，不修改原件或重写 hash 来消除告警。
- `.env`、费用账本和含本机路径的运行元数据留在本地，不随 Git 同步。后续真实调用须在新机器单独配置个人密钥，并核对/恢复相关费用状态。
- 上轮自然语言验收额度已用满 **12/12 次请求**。继续开发不等于批准新的付费请求；真实模型验收前另行明确问题范围、预算和调用上限。不得复制旧授权、新建空账本或更换输出目录来重置额度。
- 继续使用个人 GitHub 身份与个人 noreply 提交邮箱，凭据、内部信息和本机身份路径不入库。界面按正常桌面视口验收，不主动切换手机/H5 视口。

## 代码与记录入口

- [数据溯源、更新及多模态设计说明](docs/DATA_AGENT_DATA_LINEAGE_AND_UPDATES.zh-CN.md)
- [来源范围与文本读取完成报告](docs/DATA_AGENT_SOURCE_SCOPE_REPORT.zh-CN.md)
- [统一证据包完成报告](docs/DATA_AGENT_EVIDENCE_PACKAGE_REPORT.zh-CN.md)
- [任务计划与取证进度报告](docs/DATA_AGENT_TASK_PROGRESS_REPORT.zh-CN.md)
- [取证完成检查报告](docs/DATA_AGENT_TASK_COMPLETION_REPORT.zh-CN.md)
- [自然语言查询验收报告](docs/DATA_AGENT_NL_CANARY_REPORT.zh-CN.md)
- [文档阅读器](src/uteki/agents/document_reader.py)、[阅读上下文分组](src/uteki/agents/reading_groups.py)
- [Data Agent 执行循环](src/uteki/agents/data_query_agent.py)、[规划契约](src/uteki/domain/research_data/agent_query.py)
- [查询与计算执行](src/uteki/infrastructure/research_data/query_service.py)、[数据契约](src/uteki/domain/research_data/query_contract.py)
- [快照构建](src/uteki/infrastructure/research_data/query_dataset.py)、[显式输入配置](src/uteki/infrastructure/research_data/query_inputs.py)
- [已登记展示数据](data/query_views/nl-canary-v1.json)

继续开发时先验证文本与数字混合问题，再验证另一家公司和缺资料场景。两条单能力 query 已真实跑通；沿用 `task-query-canary-v1/budget.sqlite`，历史费用与失败结果不重置。每完成一项，更新勾选状态、可直接查看的步骤产物和未解决边界。页面交用户 review，不默认展开浏览器验收。
