# 开放问题实测 · 待人工审核

问题与答案未预设 Search / Cloud 等业务结论或证据位置。沿用已有引擎和固定材料集，Single / Team 并行运行、上下文独立。不是跨模型对照，不是历史预测盲测，也不是等费用比较。

## 初步结论

两组都能形成增长驱动、风险和限制的候选答案，但本次均未通过严格引用校验，不能当作已验证研究。

- Single：7 次模型调用；16 次工具调用；约 31.8 秒；21 处引用校验失败。主要使用 FY2024，却在限制中把材料称为 FY2025，未充分使用更近材料。多处将省略号改写当作精确引用。业务排序的依据仍偏向历史规模。
- Team：15 次模型调用；约 84.2 秒；41 处引用校验失败。大量引文包含额外引号或省略改写，不能将这个错误数量直接当作事实错误数。分部业务与跨业务 AI 主题并列排序，有重复维度；部分“重要性排序”被标成事实。团队阶段未修复引用问题。
- 两组均说明估值/预期资料不足，未输出涨跌幅，这是有效的边界控制。但没有完整检索某类资料，不等于整个可用材料集没有该资料。对于 Team 声称缺少分部利润率细节等内容，需要进一步核查，不能直接接受其缺失声明。
- 运行日志自身的限制：Team 计数器为 32 次工具尝试，但只有 26 条工具 JSON 文件，当前引擎某些异常/预算拒绝路径不写日志。不能宣称工具尝试日志完整。模型请求费用起止记录均保留。此问题需在后续引擎版本修复，本轮不改写历史记录。

## 费用

Single 未计缓存折扣的列表价估算：$0.08276925。
Team 同口径估算：$0.18956025。
合计：$0.27232950。不是实际账单；沿用已有价格快照，上游路由未锁定。

## 审核重点与下一轮候选

1. 强制核对材料日期，区分未读取与不存在。
2. 引文由已读取证据定位生成，不能自行加引号/省略改写后宣称逐字匹配。
3. 区分业务规模、增量驱动、横跨业务的技术主题；审查推断是否误标事实。
4. 补齐工具失败事件日志，明确不同阶段的预算消耗。
5. 先修上述证据与执行问题，再重复同题测试，不以答案听起来合理作为验收。

本文件是运行后助手复核，不是独立人工评分。原始模型输出保持不变。
