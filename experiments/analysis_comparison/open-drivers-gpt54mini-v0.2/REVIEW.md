# v0.2 修复验证（候选结果）

本轮改为 source_block 引用：模型只选择成功读取的证据位置，应用插入完整 Block 原文（包含表格完整文本）。这是来源校验，不是语义支持校验；不应将格式错误消失解读为研究准确率提升。

保留模型 raw-output 与生成证据后的 output。未读引用仍失败，校验错误传给后续修订，并最多增加一次 citation_repair。Single 60 次工具预算、Team 90 次；相对 v0.1 改变了预算，不是单变量实验。

新增异常日志和超预算日志；既有阶段调用均记录。旧 v0.1 产物不覆盖。

已知限制：完整 Block 可能比句子引用长；数字引用仍以完整表格块提供上下文，尚未实施单元格选择契约。材料年份选择仍依赖 Agent，尚无独立日期覆盖检查。候选结果仍需人工审核。

页面已接入 /experiments。调用费用为列表价估算，不是实际账单。
