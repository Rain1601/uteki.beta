# Data Agent：直接提交 query 的真实运行结果

**两条取证问题已跑通，使用真实 DeepSeek `deepseek-flash`。** 用户只提供自然语言问题；本轮资料范围已明确选定，系统自动生成任务、执行查询/读文、检查完成条件并输出可读报告。没有给模型预写 TaskPlan 或参考答案。

| 输入问题 | 模型与程序实际做了什么 | 实际结果 | 查看产物 |
| --- | --- | --- | --- |
| 请查询 Google Cloud 2025 年营业利润率，返回收入、营业利润和计算依据，并附原文引用。 | 模型生成数值任务 → 程序生成并执行 2 条参数化 SQL → 登记公式计算 → 核验引用 → 结束 | 收入 58,705,000,000 USD；营业利润 13,910,000,000 USD；营业利润率 23.69%。3 次模型调用、1 次查询工具调用。 | [数字、SQL、参数和引用](reports/annual-cloud-margin.md) |
| 请读取 Alphabet FY2025 10-K 的 Business 章节原文，供后续 Agent 分析，并说明完整阅读覆盖和剩余未读范围。 | 模型从实际目录识别章节 → 根据已读/未读反馈连续读取 → 程序核验覆盖 → 结束 | 正文 79/79 块，章节内剩余 0 块。10 次模型调用、8 次读取工具调用。 | [逐轮覆盖](reports/annual-business-reading.md) · [完整原文](reports/annual-business-reading-source-text.md) |

数字计算依据：13,910,000,000 ÷ 58,705,000,000 × 100 = **23.69%**。年报原表按百万美元展示为 13,910 和 58,705，记录保留了单位换算与原文位置。引用定位到年报第 87 页的分部表；收入另有第 60 页出处。

唯一选定来源：[SEC 公开的 Alphabet FY2025 10-K](https://www.sec.gov/Archives/edgar/data/1652044/000165204426000018/goog-20251231.htm)。固定快照为 `query-c9e2d02426106321794973a3`；候选数据访问不代表研究采纳。

## 这次修好了什么

1. 兼容 `.env` 的 `AI_HUB_MIX_API_KEY` 和 `AIHUBMIX_API_KEY`；配置冲突会明确报错。按用户最新选择保持 DeepSeek，本轮没有调用 AiHubMix。
2. 明确告诉规划器：数值查询本身返回原文引用，不能把“附引用”擅自扩大成“读完整章”；独立数字任务优先查询。此规则不包含公司、年份或期望答案。
3. query 运行后自动生成 `report.md` 和 `report-source-text.md`，提供数字表、原文、覆盖、SQL 与参数。历史运行可用 `scripts/render_task_query_result.py` 生成独立报告。
4. 运行日志显示自动拆题、当前轮次、动作与已读数量；费用保留记录。本轮按用户最新要求采用 `record_only`，没有清空旧账本或把未知费用记作零，步数/请求/时间限制保留。

## 优化前后

| 运行 | 实际模型请求 | 结果与原因 |
| --- | --- | --- |
| live-01 | 1 次 | 计划生成 90 秒超时，无取证结果；按当时费用策略停止。 |
| live-02 数字题 | 12 次 | 模型错误增加 MD&A 全章阅读要求，读了 132/255 块，未执行数值查询。保留失败计划和未读范围。 |
| live-02 正文题 | 10 次 | 阅读 10 → 22 → 32 → 45 → 61 → 71 → 77 → 79/79；宿主同意结束。 |
| live-03 数字题 | 3 次 | 修改通用工具说明后，自动计划仅包含所需数值/计算；SQL 与引用检查通过。 |

因此累计 **26 次模型请求：25 次成功响应，1 次超时**。正文和数字分别在 live-02/live-03 验收；不是把离线模拟结果当作模型结果。原超时根因未确定，这次没有复现。

## 验证与边界

32 项定向检查通过，涵盖 query 入口、独立合成公司、模型 SDK、可读报告、密钥别名、费用记录和原 CLI；没有重复全量或浏览器验收。原始计划、每轮模型决策、工具返回、证据包及费用记录全部保留在对应运行目录。

这里完成的是两条具体问题的取证链路；不等于任意问题都能正确拆解，也不等于业务/风险分析已经完成。全文覆盖仅指选定 Business 章节，年报其它章节未因此被标记为已读。计划在执行中修订、跨运行续接、图片 OCR 和网络补充取证尚未实现。

费用明细留在本地：25 次成功响应的累计已知估算 USD 0.0739596；此前超时的预留 USD 0.0178215 仍为未知，不是确认扣款。

## 下一步 action

1. 验证一条同时需要文本与数字的问题，检查模型是否正确拆分、选工具，并让后续 Analysis Agent 基于证据作答。
2. 用另一家公司及缺资料问题检验泛化与澄清；按实际失败点修正，不再扩充未用到的架构。
