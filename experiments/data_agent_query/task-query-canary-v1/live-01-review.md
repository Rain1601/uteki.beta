# 直接提交 query 的首轮验收

当前状态：**已获授权并执行首条 query，但计划生成请求在 90 秒超时，未取得模型产物。新增 1 笔未知费用，已按约定停止；第二条未执行。** 本轮共 1 次模型请求，不能视为两题验收通过。

用户只提交自然语言问题，系统传入已经明确选定的来源范围。本轮沿用已选的 Alphabet FY2025 10-K；不向模型提供预写 TaskPlan 或参考答案。模型提出内部任务，宿主保留原问题和范围，执行前校验来源、期间与依赖。

| 次序 | Query | 要验证什么 |
| --- | --- | --- |
| 1 | 请查询 Google Cloud 2025 年营业利润率，返回收入、营业利润和计算依据，并附原文引用。 | 模型自行提出数字任务；返回匹配期间的记录、原文引用和计算，不能只看其 finish 声明 |
| 2 | 请读取 Alphabet FY2025 10-K 的 Business 章节原文，供后续 Agent 分析，并说明完整阅读覆盖和剩余未读范围。 | 模型从实际目录识别章节，并依据未读范围继续调用读取工具 |

计划草案本身不证明问题拆解完整；验收时要核对计划是否保留原问题要求，以及最终检查是否覆盖这些要求。当前不验收完整业务/风险研究结论。

- 提供方：DeepSeek，模型 deepseek-flash。只读 `GET /models` 返回 200，列表包含该模型；密钥与模型名称通过连接检查，但这不证明生成接口完成过请求。
- 本轮授权：用户回复“允许按此范围执行”。两题共享最多 12 次新增请求，包含计划生成与后续循环；估算预算 USD 0.20；保留旧未知费用，新请求再遇未知费用即停。
- 实际执行配置为 [approved-config.json](approved-config.json)，准备产物为 `prepared-approved/`。原始 `config.json` 和 `prepared/` 保留为授权前历史，不用于继续执行。
- 此前一轮的 12 次额度已用满；本轮获得了新的明确授权，旧账本及其中 1 笔未知费用未改写。
- 26 项定向检查通过（约 7 秒），包含真实 SDK + 模拟 HTTP 的计划生成、查询、结束和共享计费记录链路；这不等于真实模型验收。
- 当前项目 .env 未找到 AIHUBMIX_API_KEY 条目；此次先用 DeepSeek，不影响入口开发。

## 本轮实际发生了什么

| 步骤 | 输入 / 条件 | 实际动作与结果 | 缺口 |
| --- | --- | --- | --- |
| 来源核对 | 明确选择 Alphabet FY2025 10-K | 核验来源为 SEC 公开年报；6 条所选来源的财务观察均来自该年报 | 不扩展到未选来源 |
| 接收问题 | 第一条 Google Cloud 营业利润率 query | 保存原问题、选定范围及供模型使用的目录/Schema | 用户无需填写 TaskPlan |
| 自动拆题 | 同一计费模型；本轮最多 12 次请求；单轮 90 秒上限 | 2026-09-23 15:33:40 至 15:35:10（北京时间）请求计划生成；宿主返回 `TimeoutError`，计费层记录取消 | 未收到 proposal/plan，无法评估模型拆题质量 |
| SQL / 文本执行 | 必须先获得有效任务计划 | 未执行 SQL、计算、正文读取；第二条 query 未发出 | 两道题都没有新的取证结果 |
| 停止与留痕 | 新请求费用未知即停 | 1 次请求、0 次取证工具调用；保存原请求、计划输入、失败原因及计费记录 | 没有 usage 或供应商账单；不能记作免费请求 |
| 无生成诊断 | 不重试模型、不提交资料 | 官方 `GET /models` 返回 200，耗时约 0.20 秒，模型列表包含 deepseek-flash | 只能排除当时认证失败/模型不在列表，不能确定生成超时的根因 |

费用：本轮账本记录 **1/12 次请求**，已知估算费用为 USD 0，另有 **USD 0.0178215 预留金额处于未知状态**。这不是实际扣款金额；总费用不能表述为 USD 0。原有 1 笔未知费用保持原状。

执行前自动审批曾因担心内部资料外发而拒绝启动；核验唯一来源为[公开 SEC 年报](https://www.sec.gov/Archives/edgar/data/1652044/000165204426000018/goog-20251231.htm)、派生记录无内部资料后，重新审核获准启动。当前停止原因是新增未知费用，而非审批阻塞。

可追溯产物：

- [第一题原始请求](live-01/annual-cloud-margin/session/request.json)、[实际计划输入](live-01/annual-cloud-margin/session/planning-input.json)
- [第一题失败结果](live-01/annual-cloud-margin/session/result.json)、[费用记录](live-01/annual-cloud-margin/costs.json)、[整轮结果](live-01/result.json)
- [无生成连接检查](provider-diagnostic.json)

## 下一步（按优先级）

1. 核对这 1 次请求的供应商用量/账单，或明确授权在保留新增未知费用的前提下继续。沿用本轮账本及 12 次/USD 0.20 总上限，不将余下 11 次视作自动重试许可。
2. 获准继续后，先用同一条数字 query 完成一次计划生成与查询闭环；依据实际延迟决定是否调整单轮超时，不直接扩大问题范围或更换模型。
3. 数字题通过后再执行 Business 全文阅读题，逐步展示模型动作、已读/未读覆盖及停止原因。两题通过前不宣称真实 ReAct 验收完成。
