# Multi-query v0.1 / 六题跨文档演示

已记录 33 次工具调用、49 个去重完整 Block，实际读取 FY2022/FY2024/FY2025 年报和 2026 Q2 季报。含四张完整财务表格，数字和年份坐标经 6 项断言检查。工具输出中保留来源定位、表格结构、索引 ID。表格单位通过额外读取相邻说明取得。

这不是独立测评：同一助手具有此前对话背景；目录和已读证据在会话中共享。运行前固定了检查标准，但没有独立人工 gold answer。这一点与此前设想的完整 benchmark 流程仍有差距。不能把六个候选回答算成 6/6 通过。人工审核状态均为 pending。

## 待修问题（本轮只记录，未更改冻结输入）

1. Q3：跨页引导句没有进入列表阅读分组。本次从标题开始读取，人工补足；若只命中列表项，仍可能失去引导段。
2. Q3：• Agents 和 ◦ 子项在阅读展示中被平铺。读取文字保留符号，但 UI 没保留嵌套列表。
3. Q4：FY2025 冻结索引 #770 混合收入说明、Cloud、费用等多个主题；以圆点开头的复合大 Block 被误判为单个列表项，并扩展到下一页 #773。需先识别复合容器，再重新建立候选索引，不能靠阅读分组修复。
4. Q5：表格读取没有自动带出单位引导段。成功回答依赖额外检索，不代表接口已保证口径完整。
5. 当前审阅页的 SEC 链接使用原始 source_anchor，可能只定位到所在页；不能冒充精确单元格高亮。Block、DOM path 和 cell 坐标在调用记录中保留。

## 人工审核

- [ ] Q1：11 类列表完整，且“不限于”的限定保留。
- [ ] Q2：业务例子、独立公司描述、收入类别未混淆。
- [ ] Q3：分别支持两个年份，且不把披露变化当作业务诞生时间。
- [ ] Q4：新增披露项目与新增业务、收入规模分开。
- [ ] Q5：四个金额、年份、分部营业利润口径和百万美元单位正确。
- [ ] Q6：缺失的是本地已索引电话会材料，未声称外部不存在。

下一步优先修复 1–4 并按新版本重跑相同案例；之后由人工提前准备独立参考答案，换新问题做隔离运行。此次回答可用作人工参考草稿，但不能直接当作已审核 benchmark。
