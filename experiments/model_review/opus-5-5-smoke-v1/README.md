# AiHubMix Opus 5.5 实际调用检查

2026-09-23，用户提供准确模型 ID `claude-opus-5-5`、端点 `https://aihubmix.com/v1`，并明确要求实际测试能力。

本次准备最多两次请求：用户提供的 Hello 连通检查；成功后基于冻结候选第 73、76、84、88–91 段进行业务对象、收入类别、引用和未知项提取。第二项不输入历史模型答案或用户偏好的业务节点。

实际结果：仅执行一次 SDK 请求尝试，约 48 秒后返回 `APIConnectionError → ConnectError → ConnectError`，无 HTTP 状态码、模型回复或 token usage。自动重试为 0；提取用例未调用。不能据此判断模型 ID 有效性或提取能力。费用未知，未按零计费；不修改原 DeepSeek 46/48 账本。

此前独立的公开目录诊断曾出现 TLS hostname mismatch；本次 SDK 错误链只确认连接失败，未确定相同根因。未关闭证书验证，未使用其他端点。

文件：`run.py` 为可复跑脚本（再次执行会产生新的付费请求尝试），`hello.request.json` 为实际请求，`attempts.jsonl` 为追加式调用记录。原始凭据不写入产物。

下一步：恢复此端点的可信连接后，重跑连通检查，再执行已准备的提取题并逐条核对引用、遗漏和未知处理。当前没有模型能力评分。
