[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "uteki-eval"
version = "0.1.0"
description = "Evaluation kernel for Uteki"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
uteki-eval = "uteki_eval.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]

